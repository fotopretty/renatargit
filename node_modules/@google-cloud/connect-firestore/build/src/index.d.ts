import { Firestore } from '@google-cloud/firestore';
import { Store } from 'express-session';
export interface StoreOptions {
    dataset: Firestore;
    kind?: string;
}
export declare class FirestoreStore extends Store {
    db: Firestore;
    kind: string;
    constructor(options: StoreOptions);
    get: (sid: string, callback: (err?: Error | null | undefined, session?: Express.SessionData | undefined) => void) => void;
    set: (sid: string, session: Express.SessionData, callback?: ((err?: Error | undefined) => void) | undefined) => void;
    destroy: (sid: string, callback?: ((err?: Error | undefined) => void) | undefined) => void;
}
